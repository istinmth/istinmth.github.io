function updateValue(value) {
    document.getElementById('range-value').textContent = value;
    // This just shows the value of the range input in the HTML in actual numbers, not essential but nice to have.
}